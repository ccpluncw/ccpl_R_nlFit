% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinalNumberLineFlex_mc.r
\name{ordinalNumberLineFlex_mc}
\alias{ordinalNumberLineFlex_mc}
\title{Runs the ordinalNumberLine simulation in parallel with flexible parameters.}
\usage{
ordinalNumberLineFlex_mc(
  targets,
  parList,
  loops = 1000,
  verbose = FALSE,
  numCores = NULL
)
}
\arguments{
\item{targets}{A vector of numbers representing the values to be estimated.}

\item{parList}{A list containing parameter names and their values for the
\code{ordinalNumberLine} analysis. Required elements are: \code{firstEstimate},
\code{upperBound}, \code{lowerBound}, \code{memoryLength}, \code{numberSensitivity},
\code{accuracyPercent}, \code{pIncludeConceptualPoints}, \code{visibleReferencePoints},
\code{conceptualReferencePoints}, and \code{targetOrder}.}

\item{loops}{An integer specifying the number of simulation runs to perform.
Each run can be executed in parallel, potentially speeding up the overall
estimation process. Higher values generally lead to more stable and precise
estimates but increase computation time. Loops are collapsed to 1 when the run
is deterministic, that is, when the target order is fixed or drawn once and
there is no conceptual-point inclusion draw to average over. Default = 1000.}

\item{verbose}{A logical value indicating whether to print intermediate steps
for debugging purposes. Default = FALSE.}

\item{numCores}{An integer specifying the number of CPU cores to use for parallel
execution. If \code{NULL} (default), it will use all available cores minus one.}
}
\value{
A dataframe containing the target value (\code{target}) and the mean of the
predicted estimates (\code{fEst}) across all parallel simulation runs.
}
\description{
This function executes the ordinalNumberLine simulation multiple times in parallel
using the specified parameters to generate estimates for a set of target values.
Parallel processing can significantly reduce computation time for a large number of loops.
}
\examples{
\dontrun{
  # Example with 2 cores
  my_targets <- c(2, 3, 4, 5, 6)
  my_parList <- list(
    firstEstimate = 0.5,
    upperBound = 100,
    lowerBound = 0,
    memoryLength = 3,
    accuracyPercent = 0.5,
    numberSensitivity = 1,
    pIncludeConceptualPoints = 0.2,
    visibleReferencePoints = c(0, 50, 100),
    conceptualReferencePoints = c(25, 75),
    targetOrder = "random"
  )
  parallel_results <- ordinalNumberLineFlex_mc(
    targets = my_targets,
    parList = my_parList,
    loops = 100,
    verbose = FALSE,
    numCores = 2
  )
  print(parallel_results)

  # Example using default number of cores
  parallel_results_default_cores <- ordinalNumberLineFlex_mc(
    targets = my_targets,
    parList = my_parList,
    loops = 100,
    verbose = FALSE
  )
  print(parallel_results_default_cores)
}
}
\keyword{number-line}
\keyword{ordinal}
\keyword{ordinalNumberline}
\keyword{parallel}
